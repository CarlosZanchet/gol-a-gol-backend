-- CreateTable
CREATE TABLE "Time" (
    "id" BIGINT NOT NULL,
    "nome_popular" TEXT NOT NULL,
    "sigla" TEXT NOT NULL,
    "escudo" TEXT NOT NULL,

    CONSTRAINT "Time_pkey" PRIMARY KEY ("id")
);
